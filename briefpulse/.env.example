# Paste real values into briefpulse/.env (copy this file). Keys are never committed.
#
# GEMINI_API_KEY — free tier via Google AI Studio: https://aistudio.google.com/apikey
# YOUTUBE_API_KEY — YouTube Data API v3 (official):
#   Overview: https://developers.google.com/youtube/v3/getting-started
#   search.list: https://developers.google.com/youtube/v3/docs/search/list
#   Enable API: https://console.cloud.google.com/apis/library/youtube.googleapis.com
# Each search.list costs 100 quota units/day (default project budget often 10,000). Tune:
# YOUTUBE_MAX_SEARCH_QUERIES_PER_RUN=5
# OPENAI_API_KEY — https://platform.openai.com/api-keys (billing/credits per OpenAI policy; not “free forever”)
#
OPENAI_API_KEY=
GEMINI_API_KEY=
YOUTUBE_API_KEY=
YOUTUBE_MAX_SEARCH_QUERIES_PER_RUN=5
# SerpApi — Google Trends API: https://serpapi.com/google-trends-api
SERPAPI_API_KEY=
# Tunisia geo; RELATED_QUERIES + RELATED_TOPICS = 2 searches/run by default (free tier ~250/mo).
SERPAPI_GOOGLE_TRENDS_GEO=TN
SERPAPI_GOOGLE_TRENDS_HL=fr
SERPAPI_GOOGLE_TRENDS_DATE=today 3-m
SERPAPI_GOOGLE_TRENDS_MAX_REQUESTS_PER_RUN=2
# Set true + raise MAX_REQUESTS_PER_RUN to 3+ to add one TIMESERIES call (up to 5 queries in seed_sources).
SERPAPI_GOOGLE_TRENDS_INCLUDE_TIMESERIES=false
COLLECTION_INTERVAL_MINUTES=15
# Si un flux RSS / site pose problème de certificat SSL, false désactive la vérif (dév uniquement).
# REQUESTS_VERIFY_SSL=false
DATABASE_PATH=backend/data/briefpulse.db
